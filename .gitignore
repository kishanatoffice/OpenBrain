# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Virtual environments
.venv/
venv/
env/

# Local tooling / editor state
.claude/
.DS_Store

# Local runtime data — never commit the user's brain
# (defaults live in ~/.myagent, but guard against local copies / test artifacts)
*.db
*.sqlite
*.sqlite3
*.bak
.myagent/
